export const Col = ({ children }) => do {
  forward: className;
  display: flex;
  flexDirection: column;
  flex: 1;

  <this>{children}</this>
}

export const Row = ({ children }) => do {
  forward: className;
  display: flex;
  flexDirection: row;
  flex: 1;
  
  <this>{children}</this>
}